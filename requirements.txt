# --- Core ---
python-dotenv==1.0.1
websockets==12.0
asyncio-mqtt==0.16.2 ; python_version >= "3.10"  # optional, remove if unused

# --- Wake word ---
openwakeword==0.6.0
sounddevice==0.4.7
numpy==1.26.4

# --- Speech-to-text ---
faster-whisper==1.0.3        # local/offline Whisper (STT_ENGINE=whisper-local)
SpeechRecognition==3.10.4    # fallback (STT_ENGINE=google)
pyaudio==0.2.14

# --- Text-to-speech ---
pyttsx3==2.90                # offline TTS (TTS_ENGINE=pyttsx3)
edge-tts==6.1.12              # natural online TTS (TTS_ENGINE=edge-tts)
elevenlabs==1.9.0             # premium TTS (TTS_ENGINE=elevenlabs)
playsound==1.3.0              # simple cross-platform audio playback for edge-tts/elevenlabs

# --- LLM brain ---
anthropic==0.34.2
openai==1.47.0

# --- Gesture control ---
opencv-python==4.10.0.84
mediapipe==0.10.14

# --- Skills ---
requests==2.32.3
beautifulsoup4==4.12.3       # HTML parsing for web_search_skill

# --- Dev / quality ---
pytest==8.3.2
black==24.8.0
